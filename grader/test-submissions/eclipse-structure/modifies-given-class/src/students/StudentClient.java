package students;

public class StudentClient {
    public static String fullName(Student student) {
        // incorrectly solved by modifying the Student class
        return student.getFullName();
    }
}
